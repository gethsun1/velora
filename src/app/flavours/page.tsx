'use client';import {useState} from 'react';import {SubPage} from '@/components/SubPage';import {flavours} from '@/lib/data';
export default function Flavours(){const [cat,setCat]=useState('All');return <SubPage eyebrow="THE FLAVOUR EDIT" title={<>Discover your <i>signature.</i></>} copy="Layered blends and refined classics, freshly prepared for your chosen experience."><section className="content"><div className="filters">{['All','Signature blend','Fresh','Tropical','Citrus','Classic'].map(x=><button key={x} onClick={()=>setCat(x)} className={'chip '+(cat===x?'active':'')}>{x}</button>)}</div><div className="flavourRow" style={{gridTemplateColumns:'repeat(3,1fr)',flexWrap:'wrap'}}>{flavours.filter(x=>cat==='All'||x.cat===cat).map((x,i)=><article key={x.name}><span>0{i+1}</span><small>{x.cat}</small><h3>{x.name}</h3><p>{x.note}</p><p>Intensity · {x.level}</p></article>)}</div></section></SubPage>}
