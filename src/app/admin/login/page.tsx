'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import {Logo} from '@/components/Site';
export default function Login(){const r=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState('');function submit(e:React.FormEvent){e.preventDefault();if(email==='admin@veloraexperience.co.ke'&&password==='VeloraDemo#2026'){localStorage.setItem('velora_admin','true');r.push('/admin')}}return <main className="loginPage"><div className="loginVisual"/><div className="loginBox"><form className="loginForm" onSubmit={submit}><Logo/><div className="eyebrow" style={{marginTop:55}}>OPERATIONS CONSOLE</div><h1>Welcome back.</h1><p>Sign in to manage today’s experiences, teams and collections.</p><div className="demoHint">Demo presentation mode is enabled.<br/><button type="button" className="chip" style={{marginTop:10,color:'white'}} onClick={()=>{setEmail('admin@veloraexperience.co.ke');setPassword('VeloraDemo#2026')}}>Use demo credentials</button></div><div className="field"><label>Email address</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></div><button className="button wide">Enter operations console <span>→</span></button></form></div></main>}
