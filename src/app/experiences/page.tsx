'use client'; import {useState} from 'react'; import Link from 'next/link'; import {SubPage} from '@/components/SubPage'; import {packages,money} from '@/lib/data';
export default function Experiences(){const [filter,setFilter]=useState('All experiences');return <SubPage eyebrow="CURATED FOR EVERY OCCASION" title={<>Find your <i>experience.</i></>} copy="Thoughtfully composed packages for quiet evenings, lively celebrations and everything in between."><section className="content"><div className="filters">{['All experiences','3–6 hours','Evening','Events','1–2 setups'].map(x=><button key={x} onClick={()=>setFilter(x)} className={'chip '+(filter===x?'active':'')}>{x}</button>)}</div><div className="catalog">{packages.map((x,i)=><Link href={'/experiences/'+x.slug} className={'package '+x.tone} key={x.slug}><span>0{i+1}</span><div><small>{x.tag}</small><h3>{x.name}</h3><p>{x.meta}</p><b>From {money(x.price)} →</b></div></Link>)}</div></section></SubPage>}
