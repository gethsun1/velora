import {Controller,Get} from '@nestjs/common';import {PrismaService} from './prisma.service';
@Controller('health') export class HealthController{constructor(private db:PrismaService){}@Get()health(){return{status:'ok',service:'velora-api',timestamp:new Date().toISOString()}}@Get('live')live(){return{status:'ok'}}@Get('ready')async ready(){await this.db.$queryRaw`SELECT 1`;return{status:'ready',database:'connected'}}}
