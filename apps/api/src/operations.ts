import {BadRequestException,Body,Controller,Get,Injectable,Param,Patch,Post,UseGuards} from '@nestjs/common';import {AdminGuard} from './auth.guard';import {BookingStatus,InventoryStatus} from '@prisma/client';import {PrismaService} from './prisma.service';
@Injectable() export class OperationsService{constructor(private db:PrismaService){}async analytics(){const [bookings,available,total,revenue]=await Promise.all([this.db.booking.groupBy({by:['status'],_count:true}),this.db.inventoryAsset.count({where:{status:'AVAILABLE'}}),this.db.inventoryAsset.count(),this.db.payment.aggregate({where:{status:'PAID'},_sum:{amount:true}})]);return{bookings,revenue:revenue._sum.amount||0,availableInventory:available,inventoryUtilization:total?Math.round((total-available)*100/total):0}}
 orders(){return this.db.booking.findMany({include:{customer:true,package:true,zone:true},orderBy:{createdAt:'desc'},take:100})}inventory(){return this.db.inventoryAsset.findMany({include:{category:true,reservations:{where:{releasedAt:null}}},orderBy:{code:'asc'}})}
 async status(id:string,status:BookingStatus,actorId?:string){return this.db.$transaction(async tx=>{const booking=await tx.booking.update({where:{id},data:{status,history:{create:{status}}}});await tx.auditLog.create({data:{actorId,action:'BOOKING_STATUS_CHANGED',entity:'Booking',entityId:id,metadata:{status}}});return booking})}
 async reserve(bookingId:string,assetId:string){return this.db.$transaction(async tx=>{const booking=await tx.booking.findUniqueOrThrow({where:{id:bookingId}});const end=new Date(booking.deliveryAt.getTime()+booking.durationHours*3600000);const conflict=await tx.inventoryReservation.findFirst({where:{assetId,releasedAt:null,startsAt:{lt:end},endsAt:{gt:booking.deliveryAt}}});if(conflict)throw new BadRequestException('Asset already reserved for an overlapping booking');const reservation=await tx.inventoryReservation.create({data:{assetId,bookingId,startsAt:booking.deliveryAt,endsAt:end}});await tx.inventoryAsset.update({where:{id:assetId},data:{status:'RESERVED'}});return reservation})}
 async assetStatus(id:string,status:InventoryStatus){return this.db.inventoryAsset.update({where:{id},data:{status}})}}
@UseGuards(AdminGuard) @Controller('admin') export class OperationsController{constructor(private s:OperationsService){}@Get('analytics')analytics(){return this.s.analytics()}@Get('orders')orders(){return this.s.orders()}@Patch('orders/:id/status')status(@Param('id')id:string,@Body('status')s:BookingStatus){return this.s.status(id,s)}@Get('inventory')inventory(){return this.s.inventory()}@Post('inventory/reserve')reserve(@Body()b:{bookingId:string;assetId:string}){return this.s.reserve(b.bookingId,b.assetId)}@Patch('inventory/:id/status')asset(@Param('id')id:string,@Body('status')s:InventoryStatus){return this.s.assetStatus(id,s)}}
