import {BadRequestException,Body,Controller,Get,Injectable,NotFoundException,Param,Post} from '@nestjs/common';import {IsArray,IsBoolean,IsDateString,IsEmail,IsInt,IsOptional,IsString,Max,Min} from 'class-validator';import {randomInt} from 'crypto';import {PrismaService} from './prisma.service';
export class QuoteDto{@IsString() packageId!:string;@IsString() zoneId!:string;@IsInt() @Min(1) @Max(10) setups!:number;@IsInt() @Min(3) @Max(24) durationHours!:number;@IsArray() flavourIds!:string[];@IsArray() addOnIds!:string[];@IsOptional() @IsString() promotionCode?:string}
export class BookingDto extends QuoteDto{@IsString() name!:string;@IsString() phone!:string;@IsOptional() @IsEmail() email?:string;@IsDateString() deliveryAt!:string;@IsString() eventType!:string;@IsInt() guestCount!:number;@IsString() locationType!:string;@IsString() building!:string;@IsOptional() @IsString() landmark?:string;@IsBoolean() ageAccepted!:boolean;@IsBoolean() termsAccepted!:boolean;@IsBoolean() privacyAccepted!:boolean;@IsOptional() @IsBoolean() marketingAccepted?:boolean}
@Injectable() export class PricingService{constructor(private db:PrismaService){}async quote(dto:QuoteDto){const [pkg,zone,flavours,addons,promo]=await Promise.all([this.db.experiencePackage.findUnique({where:{id:dto.packageId},include:{durations:true}}),this.db.serviceZone.findUnique({where:{id:dto.zoneId}}),this.db.flavour.findMany({where:{id:{in:dto.flavourIds},active:true}}),this.db.addOn.findMany({where:{id:{in:dto.addOnIds},active:true}}),dto.promotionCode?this.db.promotion.findUnique({where:{code:dto.promotionCode}}):null]);if(!pkg||!pkg.active)throw new BadRequestException('Package unavailable');if(!zone||!zone.active)throw new BadRequestException('Service zone unavailable');const duration=pkg.durations.find(x=>x.hours===dto.durationHours)?.price??Math.max(0,dto.durationHours-pkg.includedHours)*850;const setup=Math.max(0,dto.setups-pkg.includedSetups)*2500;const flavour=flavours.reduce((n,x)=>n+x.surcharge,0);const addOns=addons.reduce((n,x)=>n+x.price,0);const subtotal=pkg.basePrice+duration+setup+flavour+addOns+zone.deliveryFee+zone.setupFee;const discount=promo&&promo.active&&subtotal>=promo.minimumOrder?(promo.type==='PERCENT'?Math.floor(subtotal*promo.value/100):promo.value):0;const deposit=dto.setups*1500;const total=subtotal+deposit-discount;return{currency:'KES',lines:{package:pkg.basePrice,duration,additionalSetups:setup,premiumFlavours:flavour,addOns,delivery:zone.deliveryFee,setup:zone.setupFee,deposit,discount:-discount},total}}}
@Injectable() export class BookingsService{constructor(private db:PrismaService,private pricing:PricingService){}async create(dto:BookingDto){if(!dto.ageAccepted||!dto.termsAccepted||!dto.privacyAccepted)throw new BadRequestException('Required consent missing');const quote=await this.pricing.quote(dto);return this.db.$transaction(async tx=>{let customer=await tx.customerProfile.findUnique({where:{phone:dto.phone}});if(!customer)customer=await tx.customerProfile.create({data:{name:dto.name,phone:dto.phone,email:dto.email}});const reference=`VLR-${new Date().getFullYear().toString().slice(-2)}${String(new Date().getMonth()+1).padStart(2,'0')}-${randomInt(1000,9999)}`;const booking=await tx.booking.create({data:{reference,trackingPin:String(randomInt(1000,9999)),customerId:customer.id,packageId:dto.packageId,zoneId:dto.zoneId,status:'AWAITING_CONFIRMATION',setups:dto.setups,durationHours:dto.durationHours,deliveryAt:new Date(dto.deliveryAt),eventType:dto.eventType,guestCount:dto.guestCount,locationType:dto.locationType,addressSnapshot:{building:dto.building,landmark:dto.landmark},pricingSnapshot:quote,total:quote.total,flavours:{create:dto.flavourIds.map(id=>({flavourId:id,surchargeSnapshot:0}))},addOns:{create:dto.addOnIds.map(id=>({addOnId:id,priceSnapshot:0}))},history:{create:{status:'AWAITING_CONFIRMATION'}},consents:{create:[{type:'AGE',accepted:true,version:'2026-07'},{type:'TERMS',accepted:true,version:'2026-07'},{type:'PRIVACY',accepted:true,version:'2026-07'},{type:'MARKETING',accepted:!!dto.marketingAccepted,version:'2026-07'}]},notifications:{create:{channel:'IN_APP',event:'BOOKING_RECEIVED',recipient:dto.phone,message:`Booking ${reference} received`}}},include:{package:true,zone:true}});return booking})}
 async track(reference:string,credential:string){const b=await this.db.booking.findUnique({where:{reference},include:{customer:true,package:true,zone:true,history:{orderBy:{createdAt:'asc'}}}});if(!b||!(b.trackingPin===credential||b.customer.phone===credential))throw new NotFoundException('Booking not found');return{reference:b.reference,status:b.status,package:b.package.name,zone:b.zone.name,deliveryAt:b.deliveryAt,total:b.total,paymentStatus:b.paymentStatus,history:b.history.map(x=>({status:x.status,at:x.createdAt}))}}
}
@Controller() export class BookingsController{constructor(private pricing:PricingService,private bookings:BookingsService){}@Post('pricing/quote')quote(@Body() d:QuoteDto){return this.pricing.quote(d)}@Post('bookings')create(@Body() d:BookingDto){return this.bookings.create(d)}@Get('tracking/:reference/:credential')track(@Param('reference')r:string,@Param('credential')c:string){return this.bookings.track(r,c)}}
